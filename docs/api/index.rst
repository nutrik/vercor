Python API
==========

The main API reference is curated from supported public owners so readers can
identify stable entry points without navigating implementation details. Private
``vercor._*`` modules are excluded from this reference.

Runtime and topology extension points are documented separately for advanced
integrations that need to define execution, workflow, backend, or topology
contracts.
